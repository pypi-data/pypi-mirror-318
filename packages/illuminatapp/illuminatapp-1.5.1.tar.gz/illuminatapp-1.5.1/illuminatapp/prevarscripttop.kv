<Illuminat>
MDScreen:
    name: "prevarscripttop"

    MDFloatLayout:
        id: float_layout
        Image:
            source: app.imageforhmoapicysplelogineijgo()

            size_hint: 1, 1.2
            pos_hint: {"center_x": .5, "center_y": .5}
        Image:
            source: app.imageforhmoapicaapreaavartopysaaaplelogineijgo()
            size_hint: 3,3
            pos_hint: {"center_x": .5, "center_y": .5}
            opacity: 0.15

        MDLabel:
            text: "Choose the lesson"
            font_name: app.bahnscriptlightreplace()
            font_size: "53sp"
            halign: "left"
            pos_hint: {"center_x": .53, "center_y": .83}
            color: rgba(52, 0, 231, 255)

        MDIconButton:
            icon: "arrow-left"
            pos_hint: {"center_y": .95}
            user_font_size: "30sp"
            theme_text_color: "Custom"
            text_color: rgba(26, 24, 58, 255)
            on_release:
                app.setsubjecttozero()
                root.manager.transition.direction = "left"
                root.manager.current = "home"

        Button:
            id: whichchaptertop
            text: "CH1"
            size_hint: .8, .1
            background_color: 0, 0, 0, 0
            pos_hint: {"center_x": .5, "center_y": .66}
            font_name: app.bahnscriptreplace()
            text_size: self.size
            multiline: True
            halign: 'center'
            valign: 'middle'
            on_release:
                app.gettextch1(1)
                root.manager.transition.direction = "right"
                root.manager.current = "varscript"
                app.on_button_click()
                app.reducequestiontoone()
                app.gettextpara()
                app.convertTextpara()
                app.make_mastervariable()
            canvas.before:
                Color:
                    rgb: rgba(52, 0, 231, 255)
                RoundedRectangle:
                    size: self.size
                    pos: self.pos
                    radius: [5]

        Button:
            id: whichchapter2top
            text: "CH2"
            size_hint: .8, .1
            background_color: 0, 0, 0, 0
            pos_hint: {"center_x": .5, "center_y": .53}
            font_name: app.bahnscriptreplace()
            text_size: self.size
            multiline: True
            halign: 'center'
            valign: 'middle'
            on_release:
                app.gettextch1(2)
                app.on_button_click()
                print("hello, here is my button, hello thanjk 4jhiothjtyiojhiiohyjthojtohotijh")
                app.reducequestiontoone()
                app.make_mastervariable()
                app.gettextpara()
                app.convertTextpara()
                root.manager.transition.direction = "right"
                root.manager.current = "varscript"
            canvas.before:
                Color:
                    rgb: rgba(238, 124, 15, 255)
                RoundedRectangle:
                    size: self.size
                    pos: self.pos
                    radius: [5]



        Button:
            id: whichchapter3top
            text: "CH3"
            size_hint: .8, .1
            background_color: 0, 0, 0, 0
            pos_hint: {"center_x": .5, "center_y": .4}
            font_name: app.bahnscriptreplace()
            text_size: self.size
            multiline: True
            halign: 'center'
            valign: 'middle'
            on_release:
                app.gettextch1(3)
                app.on_button_click()
                print("hello, here is my button, hello thanjk 4jhiothjtyiojhiiohyjthojtohotijh")
                app.reducequestiontoone()
                app.make_mastervariable()
                app.gettextpara()
                app.convertTextpara()
                root.manager.transition.direction = "right"
                root.manager.current = "varscript"
            canvas.before:
                Color:
                    rgb: rgba(52, 0, 231, 255)
                RoundedRectangle:
                    size: self.size
                    pos: self.pos
                    radius: [5]

        Button:
            id: whichchapter4top
            text: "CH4"
            size_hint: .8, .1
            background_color: 0, 0, 0, 0
            pos_hint: {"center_x": .5, "center_y": .27}
            font_name: app.bahnscriptreplace()
            text_size: self.size
            multiline: True
            halign: 'center'
            valign: 'middle'
            on_release:
                app.gettextch1(4)
                app.on_button_click()
                print("hello, here is my button, hello thanjk 4jhiothjtyiojhiiohyjthojtohotijh")
                app.reducequestiontoone()
                app.make_mastervariable()
                app.gettextpara()
                app.convertTextpara()
                root.manager.transition.direction = "right"
                root.manager.current = "varscript"
            canvas.before:
                Color:
                    rgb: rgba(238, 124, 15, 255)
                RoundedRectangle:
                    size: self.size
                    pos: self.pos
                    radius: [5]

        Image:
            id: prevartop3
            source: app.imageforhmoapicaapreaaaavartopysaaaplelogineijgo()
            pos_hint: {"center_x": .52, "center_y": .15}
            size_hint: .7, .7
            opacity: 0

        Image:
            id: prevartop
            source: app.imageforhmoapicaapreaaaavhjartopysaaaplelogineijgo()
            pos_hint: {"center_x": .52, "center_y": .1}
            size_hint: .7, .7
            opacity: 1

