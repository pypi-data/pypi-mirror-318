
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "random_lib_nik"
version = "0.2.0"
description = "A description of random_lib."
authors = [
    { name = "Your Name", email = "you@example.com" }
]
dependencies = [
    # ...existing dependencies...
]