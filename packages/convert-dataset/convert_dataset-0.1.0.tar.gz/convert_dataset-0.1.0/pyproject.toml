[project]
name = "convert-dataset"
version = "0.1.0"
description = "Tool to generate LLM prompt to easily convert dataset to a specific format"
readme = "README.md"
requires-python = ">=3.9.18"
dependencies = [
    "describe-dataset>=0.1.2",
]

[project.scripts]
convert-dataset = "convert_dataset.prompt_generator:main"

[tool.setuptools]
license-files = []