from setuptools import setup

setup(name='Ogem_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['Ogem_distributions'],
      zip_safe=False)
