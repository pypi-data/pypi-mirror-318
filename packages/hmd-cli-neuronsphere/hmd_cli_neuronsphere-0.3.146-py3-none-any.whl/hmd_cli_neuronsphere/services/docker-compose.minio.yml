version: "3.7"

services:
  minio:
    image: quay.io/minio/minio:RELEASE.2023-04-28T18-11-17Z
    command: server --console-address ":9001" /data
    container_name: minio
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - neuronsphere_default
    volumes:
      - "${HMD_HOME}/data/librarians:/data"
