[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spurelations"
version = "0.1.0"
description = "Download spurious correlations from tylervigen.com"
readme = "README.md"
authors = [{ name = "Your Name", email = "your.email@example.com" }]
license = { file = "LICENSE" }
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
keywords = ["spurious", "correlations", "data", "visualization"]
dependencies = ["beautifulsoup4", "requests", "tqdm", "colorama"]
requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/yourusername/spurelations"
Repository = "https://github.com/yourusername/spurelations.git"

[project.scripts]
spurelations = "spurelations.main:main"
