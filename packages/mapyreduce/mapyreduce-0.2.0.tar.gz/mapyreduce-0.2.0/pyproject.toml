[tool.poetry]
name = "mapyreduce"
version = "0.2.0"
description = "Lightweight and extensible library to execute MapReduce-like jobs in Python"
authors = ["Samuele95 <strsml095@gmail.com>"]
readme = "README.md"
license = "MIT"
repository = "https://github.com/Samuele95/mapyreduce"

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^8.3.4"
multiprocess = "^0.70.17"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
