[project]
name = "hekian"
version = "0.0.1"
description = "Small library to facilitate test-time-compute!"
readme = "README.md"
requires-python = ">=3.10"

dependencies = ["sqlite3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.build]
package-dir = "src"
