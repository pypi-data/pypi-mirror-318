[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pywrkstinfo"
version = "0.1.0"
description = "Conceptual workstation information output."
authors = [
    { name = "bitdruid", email = "bitdruid@outlook.com" }
]
license = { file = "LICENSE" }
readme = "README.md"
keywords = ["bash", "shell", "tracking", "logging"]
classifiers = [
    "Programming Language :: Python :: 3.6",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.6"

[project.scripts]
blkinfo = "pywrkstinfo:main"

[project.urls]
Homepage = "https://github.com/bitdruid/forensic_concepts"