[project]
name = "empty"
version = "0.1.0"
description = "Empty example application with just dorm & django dependencies"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1,<5.2",
    "dorm-project",
]
