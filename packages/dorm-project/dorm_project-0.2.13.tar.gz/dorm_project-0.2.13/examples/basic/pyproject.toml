[project]
name = "basic"
version = "0.1.0"
description = "Basic example application using dorm"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1,<5.2",
    "dorm-project",
]
