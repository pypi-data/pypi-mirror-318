

# loading a datasets
ds = dataset_load(uuid)

# load many
ds_set = dataset_load_many([uuid1, uuid2, ...])


results = dataset.search("some description", start_date=None, stop_date=None, sample = "a", )

# search
