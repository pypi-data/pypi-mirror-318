from setuptools import setup

setup(name='Oge_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['Oge_distributions'],
      zip_safe=False)
