__version__ = '0.0.3'
__usage__ = """
    USAGE:
        As a module from any location
            mypushover [-option value]

    OPTIONS:
        -c              config file path (absolute path), if not set, use default path: site-packages/mypushover/pushover_conf.conf
        -start          start service in background
        -stop           stop service
        -restart        restart service

"""
