[project]
name = "tuler"
version = "0.1.0"
description = "CLI + config application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "colorama>=0.4.6",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
