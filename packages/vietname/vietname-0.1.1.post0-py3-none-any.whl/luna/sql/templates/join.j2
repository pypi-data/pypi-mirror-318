select
{% for expr, col in select_expressions %}
  {%+ if not loop.first %}, {% endif %}{{ expr }} as {{ quote(col) }}
{% endfor %}
from {{ base.sql_target(ctx) }} {{ quote(base.name) }}
{% for join in joins %}
left join {{ join.target.sql_target(ctx) }} {{ quote(join.target.name) }} on {{ join.condition }}
{% endfor %}
