# Documentation badges configuration
config:
    badges:
      - alt: "Documentation Status"
        img: https://readthedocs.org/projects/${projectName}/badge/?version=stable
        url: https://${projectName}.readthedocs.io/
        if: ${docInputs}
        unless: ${docRTDDisabled}
