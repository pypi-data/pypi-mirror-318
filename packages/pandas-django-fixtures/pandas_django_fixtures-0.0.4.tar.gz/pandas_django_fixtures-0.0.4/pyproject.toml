[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pandas-django-fixtures"
version = "0.0.4"
authors = [
 { name="Kirill Potapov", email="kdpotapov02@gmail.com" },
]
description = ""
readme = "README.md"
requires-python = ">=3.10.0"
classifiers = [
   "Programming Language :: Python :: 3",
   "License :: OSI Approved :: MIT License",
   "Operating System :: OS Independent",
]
dependencies = [
    "pandas>=1.0.0"
]

[tool.hatch.build.targets.wheel]
packages = ["src/pandas_django_fixtures"]
