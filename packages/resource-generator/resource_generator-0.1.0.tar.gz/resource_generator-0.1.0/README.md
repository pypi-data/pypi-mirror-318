# Resource Generator For FastApi

## Overview

The package that used for generate controller, model, service, model and schema.

## Installation

To install this package, use pip:

```bash
pip install resource-generator
