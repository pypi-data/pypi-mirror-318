[tool.poetry]
name = "analise-dados"
version = "0.1.0"
description = "Uma biblioteca simples para transformações e visualizações de dados"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.2.3"
matplotlib = "^3.10.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
