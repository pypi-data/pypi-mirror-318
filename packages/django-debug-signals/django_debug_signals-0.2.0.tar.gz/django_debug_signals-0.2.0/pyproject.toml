[project]
name = "django-debug-signals"
version = "0.2.0"
description = "Help for debugging Django signal loading order."
readme = "README.md"
authors = [
    { name = "Paul Traylor", email = "kungfudiscomonkey@gmail.com" },
]
requires-python = ">=3.8"
dependencies = [
    "django>=3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
