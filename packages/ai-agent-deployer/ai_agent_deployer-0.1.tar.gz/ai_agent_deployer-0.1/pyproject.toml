[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai_agent_deployer" 
version = "0.1"
description = "SDK for deploying AI agents"
authors = [{"name" = "custom agent deployer"}]
dependencies = [
    "cryptography",
    "huggingface_hub",
    "openai",
    "streamlit"
]