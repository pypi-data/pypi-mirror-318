
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python_example_sdk_debug"
version = "0.1.2"
description = "A description of random_lib_debug."
authors = [
    { name = "Your Name", email = "you@example.com" }
]
dependencies = [
    # ...existing dependencies...
]